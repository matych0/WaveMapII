name: "atn_pooling_feedforward"

model_class: "src.model.feedforward_mil.FeedforwardMIL"

mil_head:
  input_size: 1
  hidden_size: 512
  attention_hidden_size: 256
  output_size: 1
  dropout: true
  dropout_prob: ${training.hparams.dropout_prob}